1. In java10  they introduced new method called toUnmodifiableList(), toUnmodifiableSet() and toUnmodifiableMap().
   This method will provide a collection which is immutable.
   Means you won't be able to change it's value.
